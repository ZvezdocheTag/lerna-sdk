{
  "name": "@zvezdochetag/lerna-sdk",
  "private": true,
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "version": "2.0.0",
  "description": "Test Components project",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build",
    "test": "lerna run test"
  }
}
