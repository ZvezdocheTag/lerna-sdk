{
  "name": "lerna-sdk",
  "private": true,
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "version": "1.0.1",
  "description": "Test Components project",
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "build": "lerna run build",
    "test": "lerna run test"
  },
  "dependencies": {
    "lib-components": "https://gitpkg.now.sh/ZvezdocheTag/lerna-sdk/packages/libs?main"
  }
}
